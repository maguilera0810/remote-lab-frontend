const initialState = {
  // Aquí define el estado inicial de autenticación
  isLoggedIn: false,
  user: null,
};

const authReducer = (state = initialState, action: any) => {
  // Implementa las acciones y cambios de estado según las acciones
  return state;
};

export default authReducer;